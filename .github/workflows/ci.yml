name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Lint, type-check, test
    runs-on: ubuntu-latest
    timeout-minutes: 12
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          # Pinned (not bare `pip install ruff mypy`): unpinned installs resolve
          # whatever is newest on the day CI runs, so a new ruff/mypy release can
          # start enforcing rules mid-air with no code change to blame. Bump these
          # bounds deliberately, in the same commit as any code fixes they require.
          pip install "ruff>=0.15,<0.17" "mypy>=2.1,<3"

      - name: Ruff (lint)
        run: ruff check .

      - name: Mypy (--strict)
        run: mypy --strict bot db services core seeders

      - name: Pytest
        env:
          BOT_TOKEN: ci:dummy
          ADMIN_IDS: "111,222"
          DATABASE_URL: "sqlite+aiosqlite:///:memory:"
          LOG_LEVEL: WARNING
        run: pytest tests/ -v --asyncio-mode=auto

  docker:
    name: Docker build smoke
    runs-on: ubuntu-latest
    timeout-minutes: 8
    needs: quality
    steps:
      - uses: actions/checkout@v4
      - name: Build
        run: docker build -t aiogram-booking:ci .
